# Практика SQL на Stepik.org

Этот проект содержит подборку решённых мной сложных задач с платформы [Stepik.org](https://stepik.org/). Задачи охватывают ключевые темы SQL, необходимые для работы аналитика или разработчика баз данных.

## 🎯 Цель проекта
Продемонстрировать владение продвинутыми техниками SQL на реальных учебных задачах, близких к бизнес-логике.

## 🛠 Покрываемые темы и навыки
В решениях показана работа со следующими концепциями:
*   **DML-операции:** `DELETE` с использованием коррелированных подзапросов.
*   **Соединения и агрегация:** Комплексные `JOIN` (INNER, LEFT) с группировкой (`GROUP BY`) и агрегатными функциями (`SUM`, `COUNT`).
*   **Условная логика:** Использование выражения `CASE` для обработки `NULL` значений и условных вычислений.
*   **Обобщённые табличные выражения (CTE):** Применение оператора `WITH` для создания временных наборов данных и построения многоуровневых запросов.
*   **Оконные функции (Window Functions):** Использование `ROW_NUMBER()`, `RANK()`, `CUME_DIST()` и `PERCENT_RANK()` для аналитики и ранжирования без свёртки строк.

## 📂 Структура проекта
*   [`solutions.sql`](./solutions.sql) — основной файл с комментированными SQL-запросами, сгруппированными по темам.

## 💡 Как пользоваться
1.  Откройте файл [`solutions.sql`](./solutions.sql).
2.  Каждый раздел начинается с комментария, описывающего бизнес-задачу и ключевые используемые навыки.
3.  Изучите запрос, обращая внимание на структуру (подзапросы, JOIN, условия в `WHERE`/`HAVING`).

> **Примечание:** Задачи взяты с учебной платформы и используют её специфические учебные схемы данных. Ценность представляют не данные, а применённые методы решения.