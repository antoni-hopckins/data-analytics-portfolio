# Решение тестового задания для РОСМЭН

Это решение тестового задания на позицию **"Младший аналитик"** в компании РОСМЭН.

📋 **Полный текст задания можно посмотреть здесь:** [rosmen_task_description.md](./rosmen_task_description.md)

## 🎯 Задание 1: Расширенный анализ в Excel/Google Sheets

**Что нужно было сделать:**
1. Добавить в основную таблицу расчетные столбцы (средняя цена, продажи, остатки).
2. Рассчитать оборачиваемость товаров.
3. Вычислить общую сумму товарного запаса в рублях.
4. Отсортировать и оформить отчет.

**Что я сделал:**
- Использовал функции `СУММЕСЛИ`, `СУММЕСЛИМН`, `ВПР` для объединения данных из 4 таблиц.
- Рассчитал оборачиваемость по формуле: `Средний запас / (Продажи за период / Количество дней * 30)`.
- Добавил строку с динамическими итогами, которые пересчитываются при фильтрации.
- Отсортировал итоговую таблицу по объему продаж в рублях.

**Ключевой результат:**
- Общий товарный запас на 31.03.2024 составляет **94,1 млн рублей**.
- Лучшая оборачиваемость у категории **"Пальчиковые театры"** — всего 1,2 дня.

> 📁 **Файл с решением:** [rosmen_analysis.xlsx](./rosmen_analysis.xlsx) (откройте лист `Product`)

## 🎯 Задание 2: Сводная таблица

**Что нужно было сделать:**
Создать сводную таблицу в разрезе "БЕ" и "Товарная категория" с продажами, запасами и оборачиваемостью.

**Что я сделал:**
- На основе обогащенной таблицы из Задания 1 создал сводную.
- В значения добавил: сумму продаж в рублях, сумму товарного запаса в рублях.
- Добавил вычисляемое поле "Оборачиваемость" по формуле: `ТЗ в руб / (Продажи в руб / 31 * 30)`.

**Ключевой результат:**
- Наглядно видно, что **"ИГР Игрушки"** оборачиваются в среднем за 12,6 дней, а **"ИЗД"** (издания) — за 10,8 дней.
- Крупнейшая по выручке категория — **"Куклы и аксессуары"** (24% от общей выручки).

> 📁 **Сводная таблица находится в том же файле:** [rosmen_analysis.xlsx](./rosmen_analysis.xlsx) на отдельном листе.

## 🎯 Задание 3: SQL-запрос

**Что нужно было сделать:**
Написать запрос для получения товарного запаса Ozon по складам на 31.03.2024.

**Что я сделал:**
```sql
SELECT 
    Warehouse AS 'Склад',
    SUM(Amount) AS 'ТЗ, шт'
FROM Stocks
WHERE Partner = 'OZON' 
    AND Date = '2024-03-31'
GROUP BY Warehouse
ORDER BY SUM(Amount) DESC;

Пояснение запроса:

WHERE — фильтруем только партнера OZON и дату 31.03.2024.

GROUP BY — группируем записи по названию склада.

SUM(Amount) — суммируем количество товара в штуках для каждого склада.

ORDER BY ... DESC — сортируем результат по убыванию запаса.

Ключевой результат:
Запрос возвращает распределение остатков, полностью соответствующее образцу из задания. Анализ показывает яркую неравномерность в распределении товарного запаса по складам:

Топ-3 склада (Хоругвино, Санкт-Петербург, Пушкино) содержат ~48% всего запаса Ozon (37 878 шт из 79 265 шт).

На 10 крупнейших складов приходится ~93% всего запаса.

На мелких складах (Воронеж, Волгоград, Ногинск и др.) остатки минимальны, что может указывать на логистическую стратегию или особенности спроса по регионам.